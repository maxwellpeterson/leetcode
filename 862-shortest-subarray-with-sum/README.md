# 149. Max Points on a Line

**Difficulty:** Hard

**Runtime:** O(n), 16th percentile

**Memory:** O(n), 100th percentile

**Note:** Reduce runtime by implementing deque with array-like structure instead of linked list
